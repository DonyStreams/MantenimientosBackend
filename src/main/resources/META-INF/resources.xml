<?xml version="1.0" encoding="UTF-8"?>
<resources>
    <Resource id="inacifDataSource" type="javax.sql.DataSource">
        jdbcDriver = com.microsoft.sqlserver.jdbc.SQLServerDriver
        jdbcUrl = jdbc:sqlserver://${DB_HOST}:${DB_PORT};databaseName=${DB_NAME};encrypt=false;trustServerCertificate=true;loginTimeout=30;socketTimeout=60000;
        jtaManaged = true
        password = ${DB_PASSWORD}
        userName = ${DB_USER}
        initialSize = 0
        maxActive = 10
        maxIdle = 5
        minIdle = 0
        maxWait = 30000
        validationQuery = SELECT 1
        testOnBorrow = true
        testWhileIdle = true
        timeBetweenEvictionRunsMillis = 30000
        removeAbandoned = true
        removeAbandonedTimeout = 60
        logAbandoned = true
    </Resource>    
</resources>
